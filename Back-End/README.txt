To avoid size problem ( python packages size ):


1- install packages you need outside repository folder.
2- write the name of the packages you insalled it in
     installed-packages.txt.
3- when pull the repository, install used packages ( as mentioned in      first step ) if you not installed it previously.